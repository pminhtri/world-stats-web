import { Header } from '../layouts';
import { Container } from '../uiComponents';

const HomePage = () => {
  return (
    <Container>
      <Header />
    </Container>
  );
};

export default HomePage;
